/*
namespace Backend.Tests;

/// <summary>
/// AI generated
/// </summary>
public class GitCommandTests
{
    [Fact]
    public void Constructor_ShouldSetCorrectProperties()
    {
        // Arrange
        var branch = "main";
        var url = "https://github.com/user/repo.git";

        // Act
        var gitCommand = new GitCommand(branch, url);

        // Assert
        Assert.Equal("git clone -b main https://github.com/user/repo.git", gitCommand.Command);
    }
}
*/
